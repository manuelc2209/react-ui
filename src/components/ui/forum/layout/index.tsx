import React from 'react';

import { Header } from '../components/header';

import { StyledLayoutWrapper } from './styles';

export const Layout = () => {
    return (
        <StyledLayoutWrapper>
            <Header />
        </StyledLayoutWrapper>
    );
};
